# Scripts

This directory will contain operational shell scripts used to run and validate experiments.

Scripts should use Bash, fail fast, print clear messages, read configuration from files or environment variables, and avoid hardcoded machine-specific paths.

Executable scripts will be added in a later phase.
