# Configurations

This directory stores configuration files for experiments, examples and computing sites.

Recommended structure:

```text
configs/
├── examples/
├── experiments/
└── sites/
```

Configuration files should describe inputs, outputs, runtime options and site-specific settings. They should not contain execution logic.
